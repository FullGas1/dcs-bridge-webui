# Backlog — dcs-bridge-webui

Local markdown backlog. One lot = one directory `<LOT-ID>/` (`PRD.md` + `tickets/`). This file
is the hand-maintained index.

### Active lots

| Lot | Status | Description | Branch |
|-----|--------|-------------|--------|
| [`FEAT-LIVE-DEBUG-CONSOLE`](FEAT-LIVE-DEBUG-CONSOLE/PRD.md) | delivered | Turnkey local web UI for live-debugging a running DCS mission through VEAF-dcs-bridge: multi-widget Lua script editors, one-at-a-time injection queue, shared script templates, single-exe distribution. | — |
| [`FEAT-TABLE-RETURN-SERIALIZATION`](FEAT-TABLE-RETURN-SERIALIZATION/PRD.md) | delivered | A table return value is stringified to a memory reference (`table: 0x...`) by the external `dcs-bridge.lua` before it ever reaches this project. The backend instead rewrites the injected code so the DCS-side Lua serializes any table return into an indented, literal Lua table expression before that happens (ADR 0004). | — |
| [`FEAT-ADAPTIVE-LAYOUT-AND-ZOOM`](FEAT-ADAPTIVE-LAYOUT-AND-ZOOM/PRD.md) | delivered | The editor and the result each reserve a fixed ~30/8-line block regardless of actual content, and only the whole widget can be expanded. Dynamic collapsed-height sizing for both, an independent Expand per area, and a single page-wide zoom (floating control + Ctrl+scroll, 80%–200%). | `feature/adaptive-layout-and-zoom` |
| [`FEAT-BRANDING-HEADER-REDESIGN`](FEAT-BRANDING-HEADER-REDESIGN/PRD.md) | delivered | The banner read as a thin technical diagram, not a product banner. Replaced with a richer two-scene image (flight + tactical-map motifs, no DCS trademarked imagery), supplied by the user as a finished asset once agent tooling for real images turned out to be a dead end. | `feature/branding-header-redesign` |
| [`FEAT-BANNER-COMPACT-ASPECT`](FEAT-BANNER-COMPACT-ASPECT/PRD.md) | delivered | The banner's `width:100%; height:auto` scales entirely off the image's own aspect ratio, and the original ~2.36:1 asset rendered tall enough to crowd out widgets on a wide screen. Swaps in a user-supplied recomposition of the same content at ~4.68:1, no code change needed. | `feature/banner-compact-aspect` |
| [`FEAT-PAGE-SURFACE-CONTRAST`](FEAT-PAGE-SURFACE-CONTRAST/PRD.md) | delivered | The page background and every card-like surface (widgets, dialogs, dropdown, floating zoom control) share one `--bg` variable, so nothing stands out. Splits it into `--bg-page` (gray) and `--bg-surface` (near-unchanged white/dark), symmetric in both light and dark mode, both live-tuned against the running app. Also fixes CodeMirror's default active-line highlight for legibility against syntax colors. | `feature/page-surface-contrast` |

| [`FEAT-CI-EXE-RELEASE`](FEAT-CI-EXE-RELEASE/PRD.md) | delivered | No downloadable exe exists anywhere - every merge leaves the packaged binary stale until someone rebuilds it by hand, and build-from-source is actually broken for anyone but the original dev (undocumented venv dependency). Adds a CI workflow that tests, builds, and publishes the exe to a rolling `latest` GitHub Release on every merge to master; fixes `build_exe.ps1` to create its own venv; updates the README to lead with the direct download link. | `feature/ci-exe-release` |
| [`FEAT-LUA-FILE-DROP`](FEAT-LUA-FILE-DROP/PRD.md) | delivered (PR #13, #14, #15) | The only way to get a script into a widget is to paste or type it. Adds drag-and-drop of a `.lua` file from the file manager onto a widget (replace its editor, remember the file's base name) or onto the `+` button (one new pre-filled widget per file). `.lua` only, 512 KB cap, BOM stripped, aggregated self-dismissing message for skipped files. Base name only — no path/handle (ADR 0005). Verified in Chrome + Firefox. | `feature/lua-file-drop` |
| [`FEAT-DUAL-ZOOM`](FEAT-DUAL-ZOOM/PRD.md) | delivered (PR #17, #18, #19) | Revises `FEAT-ADAPTIVE-LAYOUT-AND-ZOOM`: one page-wide font-size zoom becomes two axes chosen by pointer position on `Ctrl`+scroll — over a widget zooms just that widget (CSS `zoom`, 40–250%), anywhere else zooms the page (a `--page-zoom` var scaling editor/result font + the banner, 40–200%; CSS `zoom` on a viewport-filling wrapper is a live no-op). The banner shrinks in height (centre-cropped, no side bands). Per-widget zoom persists. ADR 0006. Verified Chrome + Firefox. | `feature/dual-zoom` |
| [`FIX-EDITOR-DROP-HEIGHT`](FIX-EDITOR-DROP-HEIGHT/PRD.md) | delivered (PR #14, #15) | A past-the-cap script (dropped file, loaded template, restored session) showed in full instead of the ~30-line collapsed height. Two causes: `reportHeight()` measured stale layout → negative height CSS drops (PR #14, `Math.max(0,…)`); and `.cm-scroller` (a flex child) kept `min-height:auto` so it overflowed instead of scrolling inside the fixed-height editor (PR #15, `min-height:0`). Also makes the drop message bar sticky + accented. Found in `FEAT-LUA-FILE-DROP` ticket 05. | `fix/editor-drop-height`, `fix/editor-scroller-clip` |

| [`FEAT-SAVE-WIDGET-FILE`](FEAT-SAVE-WIDGET-FILE/PRD.md) | delivered (PR #24) | The drag-and-drop round-trip is one-way. Adds a header right-click menu: "Save as…" (native picker where it exists, download in Firefox) and "Save" (overwrite the exact dropped `.lua`, no dialog — Chromium only, via a handle captured on the drop, session-scoped). Client-only, no backend write. ADR 0007. Verified Chrome + Firefox. | `feature/save-widget-to-file` |
| [`FIX-WIDGET-ZOOM-HEADER`](FIX-WIDGET-ZOOM-HEADER/PRD.md) | delivered (PR #26) | Per-widget zoom (FEAT-DUAL-ZOOM) put CSS `zoom` on the whole `.widget`, so at 220% the header buttons became huge. Moves the zoom onto a new `.widget-body` wrapper around the editor + result; the header stays at 100%. Verified Chrome + Firefox. | `fix/widget-zoom-body-only` |
| [`FEAT-WIDGET-HEADER-CONTRAST`](FEAT-WIDGET-HEADER-CONTRAST/PRD.md) | merged (PR #21) | Style only: the widget-header buttons were the card's own colour and read as flat text. The header band is now recessed, every control sits on a light fill with a real border, and `Send` (injects into a live mission) is a deep red. Both modes. | `feat/widget-header-contrast` |
